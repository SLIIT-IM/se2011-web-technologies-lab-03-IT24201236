{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Open HTML File",
            "type": "chrome",
            "request": "launch",
            "file": "${workspaceFolder}/index.html"
        }
    ]
}